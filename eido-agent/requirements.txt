# -- PREAMBLE --
# This first line is CRUCIAL for deployment on CPU-only servers like Render.
# It tells pip to prioritize finding CPU-specific versions of packages like PyTorch.
--extra-index-url https://download.pytorch.org/whl/cpu

# -- FRAMEWORKS --
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
streamlit>=1.30.0
streamlit-ace
pydantic>=2.5.0
pydantic-settings>=2.0.0

# -- LLM & EMBEDDINGS --
# We specify 'torch' here. The index URL above will ensure the CPU version is installed.
torch>=2.0.1
transformers>=4.30.0
sentence-transformers>=2.2.2
openai>=1.3.0
google-generativeai>=0.5.0

# -- GEOSPATIAL --
geopy>=2.4.0
pydeck>=0.8.0

# -- DATA & VISUALIZATION --
pandas>=2.0.0
plotly>=5.15.0
altair>=5.0.0

# -- OCR --
pytesseract>=0.3.10
Pillow>=9.0.0

# -- UTILITIES --
python-dotenv>=1.0.0
numpy>=1.24.0
PyYAML>=6.0
scikit-learn>=1.3.0

# -- DATABASE & API COMMUNICATION --
aiosqlite
sqlalchemy[asyncio]>=2.0.0
alembic
asyncpg>=0.27.0
requests>=2.31.0
psycopg2-binary>=2.9.0

# -- AUTH (Optional but good to keep) --
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4